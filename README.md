# AI Petrovich - Персонализированный Telegram-бот на Claude API

Telegram-бот с асинхронной архитектурой на AWS Lambda и Claude API, обеспечивающий персонализированное общение с долгосрочной памятью о каждом пользователе.

## 🎯 Основные возможности

- **Персонализация** - бот помнит стиль общения, интересы и предпочтения каждого пользователя
- **Долгосрочная память** - сохраняет контекст между сессиями и создаёт профили пользователей
- **Групповые чаты** - понимает участников по именам, поддерживает треды и каналы
- **Асинхронная обработка** - через Amazon SQS для масштабируемости
- **Умная саммаризация** - сжимает длинные диалоги с сохранением личностей
- **Режимы работы** - настраиваемые режимы ответа (`/mode always|mention|off`)
- **Scope в группах** - контроль контекста (`/scope initiator|thread|hybrid`)

## 🏗️ Архитектура

```
Telegram Webhook → API Gateway → webhook_lambda
                                      ↓
                                  Amazon SQS
                                      ↓
                                 worker_lambda ← DynamoDB
                                      ↓
                              Claude API (Anthropic)
                                      ↓
                                 Telegram Bot API
```

### Компоненты

- **webhook_lambda.py** - приём webhook от Telegram, валидация и отправка в SQS
- **worker_lambda.py** - обработка сообщений, формирование контекста, генерация ответов
- **dynamo_utils.py** - работа с DynamoDB (пользователи, сообщения, профили, настройки)
- **claude_utils.py** - интеграция с Anthropic Claude API, саммаризация
- **telegram_utils.py** - отправка сообщений в Telegram
- **cleanup_function.py** - очистка старых данных (опционально, работает через TTL)

## 📊 База данных (DynamoDB)

### Таблицы

- **Users** - профили пользователей с долгосрочной памятью
- **Channels** - информация о каналах и группах
- **Threads** - треды в форумах/обсуждениях
- **Messages** - история сообщений с TTL 1 год
- **Summaries** - сводки диалогов для экономии контекста
- **Settings** - настройки режима работы для каждого чата

### Структура профиля пользователя

```json
{
  "user_id": "123456789",
  "username": "ivan_petrov",
  "profile": {
    "first_name": "Иван",
    "last_name": "Петров",
    "communication_style": "Технический, прямой, без лишних слов",
    "interests": ["Python", "AWS Lambda", "Claude API"],
    "long_term_summary": "Иван разрабатывает телеграм-бота...",
    "last_topics": ["Lambda Layer", "Docker", "DynamoDB"],
    "message_count": 156
  }
}
```

## 🚀 Развёртывание

### 1. Подготовка Lambda Layer

Lambda Layer содержит зависимости: `anthropic`, `requests`, `boto3`.

```bash
# Сборка layer через Docker (независимо от ОС)
build_layer.bat  # Windows
./build_layer.sh  # Linux/Mac

# Загрузка в AWS
aws lambda publish-layer-version \
    --layer-name telegram-claude-dependencies \
    --description "Anthropic, Requests для Python 3.11" \
    --zip-file fileb://lambda_layer.zip \
    --compatible-runtimes python3.11
```

### 2. Создание таблиц DynamoDB

```bash
# Users
aws dynamodb create-table \
    --table-name Users \
    --attribute-definitions AttributeName=user_id,AttributeType=S \
    --key-schema AttributeName=user_id,KeyType=HASH \
    --billing-mode PAY_PER_REQUEST

# Messages (с TTL)
aws dynamodb create-table \
    --table-name Messages \
    --attribute-definitions \
        AttributeName=dialog_key,AttributeType=S \
        AttributeName=timestamp,AttributeType=N \
    --key-schema \
        AttributeName=dialog_key,KeyType=HASH \
        AttributeName=timestamp,KeyType=RANGE \
    --billing-mode PAY_PER_REQUEST

aws dynamodb update-time-to-live \
    --table-name Messages \
    --time-to-live-specification Enabled=true,AttributeName=expire_at

# Аналогично создать: Channels, Threads, Summaries, Settings
```

### 3. Настройка Lambda функций

**webhook_lambda:**
- Runtime: Python 3.11
- Handler: `webhook_lambda.lambda_handler`
- Timeout: 10 секунд
- Переменные окружения:
  - `SQS_QUEUE_URL` - URL очереди SQS
  - `SQS_IS_FIFO` - "1" для FIFO очереди

**worker_lambda:**
- Runtime: Python 3.11
- Handler: `worker_lambda.lambda_handler`
- Timeout: 60 секунд
- Memory: 512 MB
- Layer: подключить созданный Lambda Layer
- Переменные окружения:
  - `ANTHROPIC_API_KEY` - API ключ Claude
  - `TELEGRAM_TOKEN` - токен бота
  - `BOT_USERNAME` - username бота (без @)
  - `BOT_ID` - ID бота
  - `BASE_SYSTEM_PROMPT` - базовый промпт для модели
  - `CLAUDE_MODEL` - модель (по умолчанию claude-sonnet-4-5-20250929)
  - `MAX_CONTEXT_TOKENS` - максимум токенов контекста (6000)
  - `MAX_OUTPUT_TOKENS` - максимум токенов ответа (800)
  - `USERS_TABLE`, `MESSAGES_TABLE`, etc. - имена таблиц DynamoDB

### 4. Настройка SQS

```bash
# Создание FIFO очереди для упорядоченной обработки
aws sqs create-queue \
    --queue-name telegram-bot-queue.fifo \
    --attributes '{
        "FifoQueue": "true",
        "ContentBasedDeduplication": "true",
        "VisibilityTimeout": "70"
    }'

# Настроить Lambda worker как event source для SQS
aws lambda create-event-source-mapping \
    --function-name worker_lambda \
    --event-source-arn <SQS_QUEUE_ARN> \
    --batch-size 1
```

### 5. Настройка Telegram Webhook

```bash
curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook" \
    -H "Content-Type: application/json" \
    -d '{
        "url": "https://<API_GATEWAY_URL>/webhook",
        "secret_token": "<RANDOM_SECRET>"
    }'
```

## 💡 Использование

### Команды бота

- `/mode always` - всегда отвечать в группе
- `/mode mention` - отвечать только при упоминании
- `/mode off` - не отвечать

- `/scope initiator` - учитывать только сообщения инициатора
- `/scope thread` - учитывать всех участников треда
- `/scope hybrid` - приоритет инициатору, но учитывать контекст других

### Примеры контекста

**Приватный чат:**
```
О собеседнике (Иван @ivan_petrov):
- Стиль общения: Технический, прямой
- Интересы: Python, AWS Lambda, Claude API
- Контекст прошлых бесед: Иван разрабатывает телеграм-бота...
- Последние темы: Lambda Layer, Docker, DynamoDB

[Иван (@ivan_petrov), ID:123456] Привет, помнишь про тот баг?
```

**Групповой чат:**
```
Участники беседы:
- Иван (@ivan_petrov), ID:123456 — текущий автор запроса
- Мария (@maria_dev), ID:789012 — участник

[Иван (@ivan_petrov), ID:123456] Как дела с проектом?
[Мария (@maria_dev), ID:789012] Все отлично, деплою на прод
```

## 🛠️ Технологии

- **Python 3.11**
- **AWS Lambda** - serverless compute
- **Amazon SQS** - асинхронная очередь сообщений
- **Amazon DynamoDB** - NoSQL база данных
- **Anthropic Claude API** - языковая модель (Sonnet 4.5)
- **Telegram Bot API** - интеграция с Telegram

## 📈 Особенности реализации

### Персонализация

- Извлечение `first_name`, `last_name` из Telegram API
- Автоматическое обновление профилей при каждом сообщении
- Создание долгосрочных сводок после 50+ сообщений
- Извлечение интересов и тем из диалогов

### Саммаризация

- **Краткая** - последние 30 сообщений, 600 токенов (каждые 12 сообщений)
- **Долгосрочная** - последние 60 сообщений, 400 токенов (каждые 50 сообщений)
- **Извлечение тем** - последние 10 сообщений, до 5 ключевых тем

### Управление контекстом

- Trim с приоритетом: сначала удаляются сообщения не-инициаторов (в hybrid mode)
- Кеширование профилей участников в рамках одного запроса
- Обогащённые префиксы с именами вместо голых ID

## 🔐 Безопасность

- Webhook секрет для валидации запросов от Telegram
- API ключи в переменных окружения (не в коде)
- `.gitignore` исключает чувствительные данные
- TTL для автоматического удаления старых сообщений

## 📝 TODO

- [ ] Добавить retry логику для Claude API
- [ ] Реализовать обработку `edited_message`
- [ ] Добавить CloudWatch метрики
- [ ] Параллельная обработка SQS records
- [ ] Unit тесты для критических функций
- [ ] Использование AWS Secrets Manager вместо env vars

## 📄 Лицензия

MIT

## 🤝 Контрибуция

Pull requests приветствуются! Для крупных изменений сначала откройте issue для обсуждения.

---

**Создано с помощью Claude Code** 🤖
